/// <reference types="node" />
import { EventEmitter } from 'events';
export interface BasePosterInterface {
    getStats: () => Promise<BotStats>;
    clientReady: () => boolean;
    waitForReady: (fn: () => void) => void;
}
export interface BasePoster {
    on(event: 'posted', listener: (stats: any) => void): any;
}
export declare class BasePoster extends EventEmitter {
    private options;
    private binds;
    private api;
    started: boolean;
    interval: NodeJS.Timeout;
    constructor(token: string, options?: PosterOptions);
    _binder(binds: BasePosterInterface): void;
    /**
     * Start the posting
     */
    start(): void;
    /**
     * Stop the posting
     */
    stop(): void;
    private _setupInterval;
    post(): Promise<void>;
}
